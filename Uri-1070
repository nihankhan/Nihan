#include <stdio.h>

int main()
{
   int i, num, count = 0;
   
   scanf("%d", &num);
   
   for(i = num; count < 6; i++)
   {
      if(i % 2 == 1)
      {
         printf("%d\n", i);
         
         count++;
      }
   }
   
   return 0;
}
